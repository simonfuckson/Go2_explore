# 探索效率调整与五级台阶停顿分析

对应现场会话：`20260912_181346_30b44d60`，地图：`room_real_20260914_11`。机器人时钟与现场日期不同，以会话 ID 和地图名关联数据。

## 实测结论

停止前一次采样覆盖约 730 秒，其中等待可执行目标约 623.5 秒、探索状态约 55.5 秒、普通障碍停车及恢复约 33.5 秒，其余为启动就绪。状态时长不能直接当作实际行走时长。

最后停顿时，SDK 仍使能，经典 MCF 请求成功，无“不迈步”保护故障；上层未发送非零运动指令。两份停顿地图离线复现中，旧选点器的 31 条可规划候选路径全部被完整机身检查拒绝。典型阻塞栅格在 `map (-2.025, -6.025)`，路径前进约 0.043 米时即需转入该栅格。

现场确认停顿位置存在五级连续台阶，经典步态遥控可上下。日志和代码支持的结论是：上层路径被障碍几何挡住，并非本次 SDK 重新掉使能。现有快照不能证明该单个阻塞栅格一定就是台阶立面，也没有记录可完整标定五级台阶的高度和踏面宽度。

## 已修改

- 原来的 20 厘米候选合并会永久丢掉同一区域内其他 5 厘米落点。现在先做区域分散搜索，再细化每个区域，所有候选仍须通过原有几何、路径、停车及到达转身检查。
- 为每个前沿保存搜索位置。受单轮检查数量限制时，下轮从未检查完的位置继续，避免反复检查队首导致后面的候选长期没有机会。
- 选点频率从 0.5 Hz 改为 1 Hz；保留每轮最多 12 次规划请求，增加 75 ms 软时间预算。已开始的 ROS 服务调用无法在预算到达时强制中断，因此实际单轮耗时可能略超预算。
- 机器人移动超过 0.10 米或转向超过 0.15 弧度后，失效旧姿态下的候选缓存。局部地图拒绝结果现在跟随局部地图阻塞栅格更新，修复原先错误查询全局地图的问题。
- “无新观测收益”的重试上限缩短至 2 秒，避免覆盖数据更新后仍等待 15 秒。
- 按“预计新观测面积 / 预计行走、转向和目标切换时间”选择目标。速度取安全配置，目标切换开销默认 2 秒；预测视野仍只参与评分。
- RViz 增加“探索效率 / 等待原因”，显示预计收益、路径长度、选点耗时、等待时间与阻塞坐标。原始选点状态保留，供日志分析。

速度上限继续为前进 0.30 m/s、转向 0.50 rad/s。外参、0.70 米雷达盲区、机身尺寸、安全门、SDK 步态及原工作空间未改动。

## 五级台阶的边界

本次没有增加自主上下楼梯能力。当前在线地图和避障链仍会保留台阶立面为障碍：地形模块的相对高度障碍阈值为 0.05 米，独立原始点云检查还会保留满足条件的竖直点簇；全局规划和安全门均不会把这些障碍视作可踩踏面。

继续提高速度、扩大自身滤波范围、直接删除台阶回波，不能让当前规划器正确评估五级台阶。后续台阶适配需要对每级高度、踏面深度、可用宽度、上下方向和入口姿态建立局部通行模型，并分别验证地图、规划器、安全门与 SDK 的行为。可以保留当前平地简化模式，单独增加台阶通行判定。遥控成功提供了底层能力证据，不能代替自主通行验收。

## 复测与回退

在机器狗终端启动新会话：

```bash
cd /home/nvidia/go2_explore_ws
./run_go2_explore explore room_eff_$(date +%Y%m%d_%H%M%S) --real
```

RViz 默认随会话启动。优先比较同一路线的有效目标等待时间、完成面积、转向次数及总耗时；本轮离线候选检查数量增加不代表全屋探索速度提高同样倍数。最后的五级台阶仍需另行适配，不能作为本版已解决项目。

当前会话结束使用 `./run_go2_explore stop`，先停车再保存。停止探索后可直接使用原 `run_go2`。

优化前版本已备份为 Git 标签 `backup-before-efficiency-20260914`，对应提交 `d17be2861220749e18d4a20055f1f28e898aa4b6`。机器狗上的改动前文件、可执行程序和清单保存在 `artifacts/efficiency_20260914/`。不要在运行中切换源码或可执行程序；回退源码后必须重新构建并确认链接完成，机器狗时钟回退可能导致仅运行普通增量构建不能更新程序。

验证记录见 `validation/efficiency_20260914.json`。原始日志和地图快照保留在机器狗工作空间的 `artifacts/efficiency_20260914/`，不随源码上传地图数据。
