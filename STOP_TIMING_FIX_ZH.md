# 目标到达时急停与自动恢复修复记录

更新日期：2026-09-14。已部署到 `/home/nvidia/go2_explore_ws`，下一次启动生效。当前真机会话已正常停车并保存；修改后仅进行了模拟底盘验证。

## 本次现场证据

本轮会话为 `20260912_185749_a966cab6`，地图为 `room_real_20260914_12`。机器人系统时间与现场日期不一致，以下使用日志原始时间戳关联事件，未修改系统时钟。

首次锁停为 `local_plan_stale`，约在首条状态记录后 100.44 秒。相邻控制台记录为：

| 原始时间戳（秒） | 事件 |
|---|---|
| 1789210773.541861400 | SDK bridge 收到前进 0.180 m/s、转向 0 的命令 |
| 1789210773.545067224 | TEB 报告 `GOAL Reached!` |
| 1789210773.545213 | 安全门锁停 `local_plan_stale` |

到达目标和锁停记录相差约 0.146 毫秒。bridge 日志中的 `raw` 是安全门输出传给 bridge 的输入，不能当成上游 `/cmd_vel_nav`。

附近状态记录的电量为 63–64%，`no_step_detected` 未触发；现场检查时里程计、TF 和感知有效，地形输出约 10 Hz。日志明确记录的首个锁停原因是局部轨迹过期，未见电量或步态保护作为此次锁停原因。

旧状态面板约每 5 秒保存一次完整状态。锁停会取消目标并清除轨迹，因此事后记录的“轨迹 0 点、年龄无穷大”不能还原触发瞬间的轨迹年龄。根据事件时间线和代码，可复现的关键问题是：规划器已经要求停车，但整形器的减速尾段仍非零；安全门尚未收到目标结束状态时，仍按“继续行驶”检查旧轨迹，造成误锁停。现有记录不足以精确重建所有回调的先后顺序。

另发现自动恢复的竞态：在锁停状态下，检查过程中收到新点云会使快照失效，旧代码可能直接写入 `health_ok=False`，却仍保留 `health_reason=healthy`，打断持续健康计时。现场日志出现过这种矛盾状态，单元测试也复现了恢复计时被重置。

停车后快照 `snapshot_1789210916418454392` 的局部地图、分类障碍点云和原始点云垂直障碍格均通过当前机身及前方停车范围检查。点云使用同时间戳机身姿态及原雷达安装位置转换；分类近场点 84 个，原始垂直障碍格 51 个。这证明该采样时刻几何条件允许恢复，不能代替连续 10 秒的运行准入，也不能证明整个等待期间一直没有障碍。

## 已修改的行为

1. 接收合法、当前目标对应且新鲜的规划停车请求后，安全门立即输出零速度，覆盖整形器的减速尾段。之后的新非零指令仍必须有新鲜轨迹和有效目标握手。
2. 碰撞预测仍使用实际整形速度检查制动范围。原始指令、点云、地图和 TF 断流仍触发保护；没有放宽轨迹超时阈值。持续收到停车请求但整形速度超过 1.5 秒仍未归零时，锁停 `shaper_stop_timeout`，禁止自动解除；重复零指令不能延长此期限。
3. 锁停期间，输入变化会在同一个检查周期内重新验证，最多 3 次、受原周期时间预算约束。无法在预算内完成时保持停车，明确报告 `collision_check_budget_exhausted`。自动恢复仍需停稳、完整停车范围可通行、连续 10 秒健康及新增地图，不解除人工停止、遥控接管和硬故障。
4. 增加急停瞬间的冻结记录，包括原始/整形/输出速度、目标与轨迹代次、数据年龄及近期目标、速度、轨迹事件。记录在取消目标前生成，后续回调不会覆盖；状态面板将其作为 `safety_stop_context` 立即写入本轮 `status_events.jsonl`。
5. RViz 新增“规划停车请求”和“自动恢复准入”，直接显示等待整形归零、等待取消目标、停车范围受阻或检查超时等具体原因。

涉及安全门、状态面板及其回归测试，均为 Python 修改，无需重新编译。前进上限 0.30 m/s、转向上限 0.50 rad/s、原步态逻辑和原工作空间保持不变。

## 验证结果

- 旧代码运行新增的 11 项回归用例，出现 5 项失败和 1 项错误；修改后安全模块 137 项、探索模块 65 项测试全部通过。
- 完整 ROS / TEB / 速度整形器 / 安全门 / 模拟 SDK 连续完成 3 个目标，捕获 9 次“规划停车但整形尾段非零”采样，没有误锁停，也没有停车请求持续有效后输出非零速度。
- 暂停整个 move_base 的断流注入测试通过：本次先检测到 `local_costmap_stale`，恢复后约 12.29 秒自动重新使能，生成新目标并恢复模拟运动；随后人工停止保持停机。原始指令超时的独立判定另由回归测试覆盖。
- 普通障碍出现后停车、清空后重新选点通过；点云持续断流保持停车通过；遥控接管后不自动重新使能通过。
- 5 组模拟联调均正常停车保存。原工作空间 1494 个现存基线文件内容、权限或链接未变；89 个历史地图文件在本轮前已缺失，未把它们计为本轮删除。新工作空间解析隔离检查通过。

机器可读结果见 [validation/stop_timing_20260914.json](validation/stop_timing_20260914.json)。现场原始日志、停车结果和测试日志保存在机器人 `artifacts/stop_analysis_20260914/`；现场快照保存在 `artifacts/efficiency_20260914/20260912_185749_a966cab6/`。

本次真机保存结果为 `saved`，PCD 和快照清单存在，进程正常退出。修改后的实际行走、物理停车、连续探索效率和自主上下五级台阶尚未复测，本记录不宣称通过。

## 下一次启动与回退

在机器狗终端使用原命令，选择新的地图名：

```bash
cd /home/nvidia/go2_explore_ws
./run_go2_explore explore room_real_20260914_13 --real
```

默认打开 RViz 和相机视频，默认保留自动恢复。另一个终端可执行 `./run_go2_explore status` 或 `./run_go2_explore stop`；没有在本次修复中启动真实 SDK 或下发真实运动。

修改前版本已上传 GitHub 标签 `backup-before-stop-fix-20260914`，对应提交 `bc8d1d575cb3039139ac50d431ada76b11c427e4`。恢复此前探索版本应在会话退出后按该标签部署；使用原导航系统只需结束探索后运行原 `run_go2`。
