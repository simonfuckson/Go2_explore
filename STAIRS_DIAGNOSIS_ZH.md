# 台阶区域现场监测与定位误锁修复

日期：2026-09-14。机器人系统时钟显示 2026-09-12，使用会话 ID 和原始时间戳关联数据。现场确认：同一处五级连续台阶，约 0.10 m 级高、0.30 m 踏深、1.95 m 宽；经典步态遥控可以上下。

## 两个独立的问题

**自主探索还没有发出上阶指令。**在 `room_real_20260914_13` 和 `room_real_20260914_15` 停顿处，SDK 保持使能，经典步态请求返回成功，感知和定位当时正常，状态是 `WAITING_FOR_GOAL`。选点器报告 `global_footprint` / `local_stop_envelope`。连续 25 秒采样中，安全门没有锁停，也没有非零输出。它是在规划和机身范围检查阶段等待，不属于底层收到前进指令却无法迈步。

冻结地图离线检查结果：

| 现场快照 | 候选数 | 实际请求规划数 | 栅格路径结果 | 梯度路径结果 |
|---|---:|---:|---|---|
| `20260912_194110_25744b6e/snapshot_00` | 488 | 158 | 158 条被全局机身范围拒绝 | 158 条被局部停车范围拒绝 |
| `20260912_200736_bbe0a554/snapshot_00` | 582 | 242 | 242 条被局部停车范围拒绝 | 242 条没有全局路径 |

以上使用相同实测地图与完整机身检查，没有发布运动指令。切换路径提取方式未产生可执行路线，因此没有部署这一参数调整。阻塞坐标可能位于接近台阶的侧向路线，不能仅凭一个栅格把它认定为台阶立面。

当前探索链没有经过验证的台阶通行模型：普通相对高度障碍阈值为 0.05 m，原始点云还检查局部垂直结构，规则可把 0.10 m 台阶保留为障碍。保存地图能看见多级踏面，与现场给出的级高、踏深基本吻合；这不能自动证明所有踏面、边缘和入口均可通行。本次没有更改障碍阈值或把台阶回波直接删除，**自主上阶尚未实现**。

**遥控过程中另有一个可复现的定位误锁。**`room_real_20260914_13` 遥控接管后，首个定位连续性错误发生在 FAST-LIO 的 `body_lio` 姿态流。原记录缺少相邻帧姿态，随后启动了仅观测会话 `20260912_201657_66b61786`，地图名 `stairs_observe_20260914_01`；真实 SDK 未加载，自动探索未启动。现场人员完成上下阶及转身、反方向上下阶，高频记录保存了整个过程。

关键相邻帧的间隔为 0.100045 秒，位置变化仅 0.02837 m，四元数计算的三维旋转为 0.18158 rad（10.4°）。FAST-LIO 中间坐标系的欧拉俯仰接近 84°，欧拉航向变化达到 0.91177 rad（52.2°）；旧检查阈值为 0.51007 rad（29.2°），因此误锁 `odometry_discontinuity`。这里的高俯仰属于中间坐标系，不能当作机器狗机身的实际俯仰。

完整记录里出现 4 组这样的欧拉航向超限：实际三维旋转约 7.7–10.7°，均未超过原旋转阈值。需要区分这次确定的表达方式问题与真正的定位跳变；不能据此取消连续性保护。

## 已部署的修改

- 使用归一化四元数的最短三维夹角比较相邻姿态，正确处理 `q` 与 `-q` 的等价性，不再使用倾斜中间坐标系的欧拉航向差作为旋转距离。原位移阈值和旋转阈值保留；纯滚转/俯仰的真实突跳也能被检查到。
- 首次定位跳变记录保存前后位置、四元数、时间间隔、实际变化量与阈值。后续消息不会覆盖第一次故障及证据。
- 默认启动被动运动记录器，保存完整里程计和 TF、每秒最多两帧完整点云、规划与控制状态。文件为本轮日志目录中的 `motion_evidence.bag` 和 `motion_evidence.json`。每轮约 128 MiB 上限；磁盘不足 512 MiB 时停止记录并报告，不删除已有数据。该记录不包含相机视频和原始 Livox 包，可替代日常诊断中的大体积全量录包，但不能代替完整传感器回归。
- RViz 增加“运动数据记录”状态。记录器不参与控制准入，记录故障不会改变运动许可。

改动仅位于独立探索工作空间。原 FAST-LIO、外参、步态、SDK 控制、运动上限与 `/home/nvidia/go2_nav_ws` 不变。新的启动集成包含 Python 程序和 launch/CMake 安装清单；现有 C++ 库没有改动。

## 验证与交付状态

- 安全模块 141 项、探索模块 65 项测试通过，新增用例覆盖本次实际四元数对、四元数符号等价、10 cm 高度变化和真实姿态突跳。
- 回放本次 2927 条 FAST-LIO 里程计及 2927 条导航里程计：旧检查锁停，新检查没有连续性故障。观测会话的全部安全输出均为零，实际移动由现场遥控完成。
- 完整模拟栈连续完成 3 个目标，捕获 16 次规划停车后的整形尾段，没有误锁停。新记录器正常保存，未丢弃队列消息。
- 独立工作空间解析和原工作空间校验通过。原基线 1494 个现存文件保持不变；89 个历史地图文件在本轮前已缺失。
- 观测会话已正常停车保存退出，地图 PCD 与清单校验有效。此前真机会话 `room_real_20260914_13` 的退出记录是 `FAULT / shutdown_save_pending`，其 PCD 存在且清单可验证，但不能把该次退出报告为正常成功。

机器可读结果见 [validation/stairs_20260914.json](validation/stairs_20260914.json)。原始台阶快照在 `artifacts/stairs_20260914/`，遥控记录在 `logs/20260912_201657_66b61786/`。

启动命令不变，选择新的地图名：

```bash
cd /home/nvidia/go2_explore_ws
./run_go2_explore explore room_real_20260914_16 --real
```

本次更新解决了倾斜坐标系导致的定位误锁，**不代表台阶已被规划器放行**。自主上阶还需将连续踏面、立面、可用宽度、入口方向和边缘约束接入地图、选点、局部规划及安全门，并单独验证；这是现有平地探索链尚缺少的能力。修改后的真实自动行走和自主上下阶均未在本次报告中宣称通过。

修改前代码已上传标签 `backup-before-stairs-20260914`，对应 `1119cb692e4bf7701fcfdf3b09b49ab2d46ff142`。
